[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that require external services (Azurite, Azure)
    e2e: end-to-end tests against deployed or full local stack
